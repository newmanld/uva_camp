<%# button for creating subcollections %>


<%= link_to label, collections.new_collection_path+"?collection_id=#{@collection.id}", :method=>:get, :class=>"btn btn-primary collection-add submits-batches", 'data-behavior'=>'hydra-collections', :id=>'hydra-collection-add' %>
